'use client';
import { useFrame } from '@react-three/fiber';
import { MutableRefObject, useMemo, useRef } from 'react';
import * as THREE from 'three';
import WatercolourMaterial from '@/components/shaders/WatercolourMaterial';
import { CameraProfile, CropBox, smooth } from '@/lib/sceneConfig';
import { qualityFor } from '@/lib/quality';
type Fragment={box:CropBox;position:[number,number,number];scale:[number,number];rotation:[number,number,number];displacement:number};
const boxes:CropBox[]=[[0,1080,420,1536],[350,1030,820,1536],[700,1060,1087,1536],[30,620,360,900],[760,560,1060,860]];
const fragments:Fragment[]=[
{box:boxes[0],position:[-5.2,-1.1,-5],scale:[5.2,4],rotation:[.12,.42,-.12],displacement:.07},{box:boxes[1],position:[4.6,1.6,-6.5],scale:[6,4.7],rotation:[-.18,-.38,.09],displacement:.08},{box:boxes[2],position:[-2.2,4.2,-7.8],scale:[5.6,5],rotation:[.4,.12,.2],displacement:.09},{box:boxes[3],position:[2.8,-3.4,-8.8],scale:[4.2,3.4],rotation:[-.28,.5,-.3],displacement:.055},{box:boxes[4],position:[5.5,3.7,-10.2],scale:[5,4],rotation:[.18,-.52,.14],displacement:.075},{box:boxes[0],position:[-5.7,2.9,-11],scale:[4.5,3.3],rotation:[-.12,.55,.22],displacement:.06},{box:boxes[1],position:[1.3,5,-12],scale:[6.5,4.3],rotation:[.55,.08,-.16],displacement:.085},{box:boxes[2],position:[-.4,-4.7,-12.7],scale:[6.2,4.8],rotation:[-.52,-.08,.18],displacement:.07},{box:boxes[3],position:[6,-.7,-13.8],scale:[4.8,3.8],rotation:[.02,-.65,-.24],displacement:.055},{box:boxes[4],position:[-6.2,-.4,-14.4],scale:[5,4],rotation:[.05,.62,.2],displacement:.07},{box:boxes[0],position:[2.4,1.2,-15.4],scale:[5.4,4.6],rotation:[.2,-.1,.48],displacement:.08}];
function PaintFragment({fragment,progressRef,index}:{fragment:Fragment;progressRef:MutableRefObject<number>;index:number}){const mesh=useRef<THREE.Mesh>(null);const base=useMemo(()=>new THREE.Vector3(...fragment.position),[fragment.position]);useFrame(({clock})=>{if(!mesh.current)return;const p=progressRef.current,enter=smooth(p,.61+index*.0015,.69+index*.0015),leave=smooth(p,.77,.86),v=enter*(1-leave);mesh.current.visible=v>.01;const s=Math.max(.001,v);mesh.current.scale.set(fragment.scale[0]*(.72+s*.28),fragment.scale[1]*(.72+s*.28),1);mesh.current.position.copy(base);mesh.current.position.x+=Math.sin(clock.elapsedTime*.08+index)*.08*v;mesh.current.position.y+=Math.cos(clock.elapsedTime*.06+index*.7)*.06*v;});return <mesh ref={mesh} position={fragment.position} rotation={fragment.rotation} visible={false} renderOrder={4}><planeGeometry args={[1,1,20,20]}/><WatercolourMaterial texture="/art/source-full.svg" crop={fragment.box} opacity={.82} displacement={fragment.displacement}/></mesh>}
export default function PaintWorld({progressRef,profile}:{progressRef:MutableRefObject<number>;profile:CameraProfile}){const count=Math.min(qualityFor(profile).paintCount,fragments.length);return <group>{fragments.slice(0,count).map((f,i)=><PaintFragment key={i} fragment={f} progressRef={progressRef} index={i}/>)}</group>}
